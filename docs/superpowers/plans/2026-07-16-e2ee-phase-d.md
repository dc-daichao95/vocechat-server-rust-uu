# E2EE Phase D (in progress) — Client binding

Date: 2026-07-16

## Done in this slice

### Flutter (`vocechat-client-uu`)

- `lib/services/e2e_v2_core.dart` — FFI to `voce_e2ee_core.dll`
- `lib/services/e2e_v2_identity.dart` — generate + store + publish v2 identity/SPK
- `lib/api/lib/e2e_api.dart` — protocol + signed prekey + device-link endpoints
- Bootstrap chooses v1 vs v2 via `GET /user/e2e/protocol`
- `windows/libs/voce_e2ee_core.dll` + CMake install next to exe

### Web (`vocechat-web-uu`)

- `public/e2e-core/*` — WASM package
- `src/app/e2e/v2_core.ts` — load WASM + generate/publish helpers
- `useE2eBootstrap` — v2 path when `e2e_protocol_ver >= 2`
- RTK endpoints: protocol + device-link

## Not yet (next slices)

- DM Double Ratchet encrypt/decrypt on send/receive (still v1 body when protocol=1)
- Channel Sender Keys via core
- Safety-number UI / block-on-key-change
- Device-link QR screens
- Secure storage hardening for Flutter desktop secrets

## Enable v2 cutover

Admin sets login config `e2e_protocol_ver = 2` after clients with this build are deployed.
